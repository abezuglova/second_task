class Task {
  final int id;
  final String name;
  final DateTime termDateTime;
  final bool isDone;

  Task({
    required this.id,
    required this.name,
    required this.termDateTime,
    required this.isDone,
  });
}
